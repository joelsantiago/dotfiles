[user]
	name = Joel Santiago
	email = joel.santiago1@gmail.com
[core]
	editor = subl -n -w
[filter "media"]
	clean = git-media-clean %f
	smudge = git-media-smudge %f
[difftool "Kaleidoscope"]
    cmd = ksdiff --partial-changeset --relative-path \"$MERGED\" -- \"$LOCAL\" \"$REMOTE\"
[diff]
    tool = Kaleidoscope
[difftool]
    prompt = false
[mergetool "Kaleidoscope"]
    cmd = ksdiff --merge --output \"$MERGED\" --base \"$BASE\" -- \"$LOCAL\" --snapshot \"$REMOTE\" --snapshot
    trustExitCode = true
[mergetool]
    prompt = false
    keepBackup = false
[merge]
    tool = Kaleidoscope
[color]
    ui = auto
[github]
    user = joelsantiago
[alias]
    a   = add --all
    b   = branch
    c   = checkout
    cb  = checkout -b
    ca  = commit -am
    cm  = commit -m
    p   = pull origin master
    pom = push origin master
    rc  = rm --cached
